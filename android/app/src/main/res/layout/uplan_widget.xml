<?xml version="1.0" encoding="utf-8"?>
<!-- The home-screen widget's appearance. Only RemoteViews-supported widgets
     (LinearLayout, TextView, ImageView, ...) are allowed here — no Flutter. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/uplan_widget_bg"
    android:padding="14dp">

    <!-- Title + date row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/widget_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Uplan"
            android:textColor="#FFFFFFFF"
            android:textSize="16sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/widget_date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#B0FFFFFF"
            android:textSize="12sp" />
    </LinearLayout>

    <!-- Today's shift (colour set at runtime) -->
    <TextView
        android:id="@+id/widget_shift"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text="Rest day"
        android:textColor="#FFFFFFFF"
        android:textSize="15sp"
        android:textStyle="bold" />

    <!-- Habits / tasks summary -->
    <TextView
        android:id="@+id/widget_counts"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text="Tap to open"
        android:textColor="#B0FFFFFF"
        android:textSize="13sp" />
</LinearLayout>
