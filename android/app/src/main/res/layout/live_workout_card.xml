<?xml version="1.0" encoding="utf-8"?>
<!-- Dashboard body while a workout is active: session name on the left,
     big elapsed-time chronometer on the right (ticks natively — zero
     re-posting). Chronometer IS on the RemoteViews whitelist. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/live_workout_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textAppearance="@style/TextAppearance.Compat.Notification.Title"
            android:textSize="15sp"
            android:maxLines="1"
            android:ellipsize="end" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:text="Workout in progress · tap to open"
            android:textAppearance="@style/TextAppearance.Compat.Notification.Info"
            android:textSize="12sp"
            android:maxLines="1" />
    </LinearLayout>

    <Chronometer
        android:id="@+id/live_workout_elapsed"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:textAppearance="@style/TextAppearance.Compat.Notification.Title"
        android:textSize="24sp"
        android:textStyle="bold" />
</LinearLayout>
