<?xml version="1.0" encoding="utf-8"?>
<!-- Cover-screen (Flex Window) variant. The Flip 6 cover screen is
     748x720px @2.125 = ~352x339dp, so the footprint is small and roughly
     square. widgetCategory includes `keyguard`, which is what Samsung's own
     cover widgets declare. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="250dp"
    android:minHeight="110dp"
    android:minResizeWidth="250dp"
    android:minResizeHeight="110dp"
    android:maxResizeWidth="350dp"
    android:maxResizeHeight="200dp"
    android:targetCellWidth="4"
    android:targetCellHeight="2"
    android:updatePeriodMillis="0"
    android:initialLayout="@layout/uplan_month_widget"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen|keyguard" />
