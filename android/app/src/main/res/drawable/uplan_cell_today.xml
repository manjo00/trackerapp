<?xml version="1.0" encoding="utf-8"?>
<!-- Rounded "today" tile (no shift): faint fill + accent ring, like the app. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#33B39DDB" />
    <stroke android:width="1.5dp" android:color="#99B39DDB" />
    <corners android:radius="10dp" />
</shape>
