# Session Status / Handoff — 2026-07-28

A living snapshot so work survives context compaction. CLAUDE.md (in OneDrive,
outside this repo) holds the durable project history; this file holds the
**in-flight** decisions and gotchas that otherwise get re-derived.

## ✅ DONE & SHIPPED
- **master = v1.9.1** (PR #1 merged, 2026-07-28). Contents:
  - Note→task **@time tokens** (schema v15): a checkbox note line starting with a
    time (`@1450pm take bloods`, `at 12:50pm july 12`) auto-creates a linked task
    in a list named after the note.
  - **`core/text/WhenParser`** (pure, 23 tests): Todoist-style NL date/time,
    reused by notes AND the task editor's "Set due" chip. Precise-not-greedy so
    clinical shorthand (`bed 7`, `35flow`, `O2 30`) is never a date.
  - **Two-way sync** (`ArchiveService`): note checkbox ⇄ task complete; delete
    task → deletes note line (no respawn); archive task → strips the @token.
  - **Notes editor polish**: delete note (⋮), delete line (✕), drag-reorder
    blocks; auto-tasks are checkbox-only.
  - **Today-marker fix**: today shows a filled chip on month calendars even when
    another day is selected (`ShiftMonthCalendar`, covers all 3 month views).
- **v1.9.1 released** to manjo00/uplan-releases (APK attached, not draft). Phone
  (R5CX631BMJB) runs v1.9.1.
- Tests 135/135, `flutter analyze` clean.

## 🔧 IN-FLIGHT (pick up here)
0. **Notes editor redesign** — **BUILT** (2026-07-29), committed on master.
   Spec: `docs/superpowers/specs/2026-07-29-notes-editor-redesign-design.md`.
   The block editor is now a clean "open note": per-line drag-handle + ✕ removed;
   bigger title + hairline divider; roomier text; light circular checkbox;
   subtler photo ✕. Reorder + delete moved into an on-demand **"Edit lines"**
   mode (AppBar ⋮). Text/checkbox also delete via backspace-on-empty
   (best-effort — soft keyboards may swallow it; Edit-lines is the guaranteed
   path). New pure `blockLabel()` helper (tested). Files: `note_editor_screen`,
   `text_block_view`, `checkbox_block_view`, `photo_block_view`,
   `domain/block_label.dart`. Tests 143/143, analyze clean. **APK built; install
   BLOCKED** — adb went "unauthorized" after a daemon restart, awaiting the phone
   "Allow USB debugging" tap. Feel (paper vs flat) still user's call after they
   see it live.
1. **Notes photo-grid redesign** — Phase 1 **BUILT** (2026-07-28), user
   confirmed "looks good inside the notebook". Plan:
   `docs/superpowers/plans/2026-07-28-notes-photo-grid.md`. Shipped: pure
   `notePreview` helper (`lib/features/notes/domain/note_preview.dart`, 4 tests)
   + `NoteGridCard` widget + `NotebookDetailScreen` now a 2-col `GridView`
   (photo-first cards; notebook-tinted cover fallback; covers notebook detail
   AND Unfiled). `note_tile.dart` deleted. Installed on phone (release, in
   place). Awaiting user's eyeball vs the "Ticu 27 july" screenshot. Phase 2
   (deferred): overview notebook covers, editor polish, list/grid toggle,
   masonry.
2. **August 2026 shift entry** for the user ("Ahmed Alzahrani" row). All **DAY**
   shifts:
   - **D1** (ICU1): Aug 1, 4, 5
   - **HC** (Home Health Care): Aug 9, 10, 11, 12, 13
   - **T** (Truma ICU): Aug 16, 17
   - **E** (Equipment): Aug 23, 24, 25, 26, 27
   - **ER** (Emergency Room): Aug 30, 31
   ⚠️ UNCONFIRMED: this reads as 17 working days but the source sheet's tally =
   15, so ~2 cells (likely in D1 or E) may be wrong — verify against the sheet
   before/while entering. Codes already exist in the user's app (rotation
   labels); only the per-day schedule needs entering.
   BLOCKED on flaky USB. Open decision: user enters manually (drawer → Work
   schedule → August → tap day → pick day + code) vs Claude drives via adb vs
   build a reusable monthly rota-import (user leaning against a new feature).

## 📋 FEEDBACK BACKLOG (feedback/app.md)
- ✅ Per-list **auto-archive done tasks** — DONE (2026-07-29). Schema **v16**
  `task_lists.autoArchiveCompleted` (default false); list ⋮ CheckedPopupMenuItem;
  `ToggleTask._maybeAutoArchive` routes a completed task through
  `ArchiveService.archiveTask` when its list opts in. DAO round-trip tested.
- ✅ **Photo edit/crop** — DONE (2026-07-29). `image_cropper` (UCrop) +
  `ImageStorageService.cropExisting` (saves a NEW filename to dodge Image.file's
  path cache) + `NotesRepository.cropPhotoBlock`; crop button on each photo.
- 🔧 Notes redesign — grid ✅, editor ✅ (flowing, no boxes; "needs some
  tinkering" per user); **rich text** (bold/color/size/H1–H3/highlight) is the
  remaining big Samsung-style piece — deferred to its own brainstorm+project.
- (today-marker: DONE in v1.9.1.)

## 🔤 Rich text Phase 1 — BUILT (2026-07-29), pending device verify
Spec `specs/2026-07-29-notes-rich-text-design.md`, plan
`plans/2026-07-29-notes-rich-text.md`. **Block-level** formatting (no inline
markers): schema **v17** adds `note_blocks.headingLevel/highlighted/bold/italic`
(+ `NoteBlockType.divider`). Widgets render heading size / bold / italic /
whole-line highlight; new `DividerBlockView`. Context-aware bottom bar —
`_FormatBar` (Body/H1/H2/H3 cycle + Bold/Italic/Highlight toggles, wrapped in
`TextFieldTapRegion` so taps don't unfocus) when a line is focused, else the add
bar with a new **Divider** button. `NotesDao.setBlockFormat` (partial update).
Pure `noteHeadingFontSize` + notePreview divider-guard tested; 151 tests green.
All committed to master; **APK built but NOT installed** (phone dropped USB).
Phase 2 (later): word-level inline, text color, font size via an editor engine.

## 🧩 Notes templates + insert-anywhere — BUILT (2026-07-29)
Spec `specs/2026-07-29-notes-templates-design.md`, plan
`plans/2026-07-29-notes-templates.md`. Shipped:
- **Keyboard-accessory toolbar**: toolbar moved out of bottomNavigationBar into
  the body (Expanded scroll + toolbar) so it rides on top of the keyboard (fixes
  the format bar hiding). Two rows in a `TextFieldTapRegion`: format row
  (H1-3/Bold/Italic/Highlight) over an always-present add row (Text/Checkbox/
  Photo/Divider/Templates).
- **Insert-after-current**: add actions insert after the focused line (via
  `insertBlockAfter`) or append when nothing's focused; new block gets focus.
- **Templates = flagged notes** (schema **v18** `notes.isTemplate`): excluded
  from `watchNotes`/`watchLastNoteEditByNotebook`; `watchTemplates()`;
  `createNote(isTemplate:)`. `NotesDao.insertBlocksAt` (batch shift-insert);
  `ImageStorageService.duplicate` (photo files never shared);
  `NotesRepository.saveAsTemplate / newNoteFromTemplate / insertTemplateInto`.
- **UI**: Notes overview **Templates** tile → `/notes/templates`
  (TemplatesScreen: list + New-template FAB + delete); notebook **＋** →
  Blank-or-template sheet; editor ⋮ **Save as template** / **Insert template**;
  add-row Templates button = insert picker.
- 155 tests (4 new: exclusion, insertBlocksAt shift, copy preserves formatting/
  order, insertTemplateInto), analyze clean. **APK built + installed on phone;
  awaiting device verify.** Phase 2 later: template categories, variables.

## ✅ RELEASED — v1.11.1 (2026-07-29)
- **v1.10.0** — photo grid + editor redesign + auto-archive + photo crop.
- **v1.11.0** — rich text (block-level headings/bold/italic/highlight + divider)
  + templates + keyboard-accessory toolbar + insert-after-current.
- **v1.11.1** — patch: photo now inserts after the cursor line (was appending);
  add-toolbar simplified (Text·Checkbox·Photo·Divider; Insert-template in ⋮).
All on master + tagged on manjo00/uplan-releases. Phone runs 1.11.1+17.
pubspec = **1.11.1+17**. Everything this session is shipped.

## 🧵 2026-08-01 — "Tame long notes" + the drag engine (BUILT, on device, UNRELEASED)
Specs: `specs/2026-08-01-notes-tame-long-notes-design.md`,
`specs/2026-08-01-notes-drag-engine-design.md`. Plan:
`plans/2026-08-01-notes-tame-long-notes.md`. All on master, **not yet released**
(pubspec still 1.11.1 — bump before cutting v1.12.0).
- **A7 collapse** (schema **v19** `note_blocks.collapsed`): fold caret per
  heading, "· N hidden", ⋮ Collapse all / Expand all, folds honoured in arrange.
- **A1 contained photos**: `kNotePhotoMaxHeight` (~220dp, `BoxFit.contain`, tap →
  full-screen). Deliberately **no toggle** — user asked for the smoothest,
  least-configurable option.
- **Outline indentation**: children indent under their heading, on the **leading
  side per line** (`lineStartsRtl` → Arabic indents from the right).
- **A8 → custom drag engine** (schema **v20** `note_blocks.indent`, backfilled):
  in-place arrange mode, clean-lift drag, fold-on-grab (a heading carries its
  bed), **easy out** (any gap → top level, never splits a bed) and **deliberate
  in** (hold over a header 500 ms → expand + highlight). Depth is stored, not
  derived, which is what makes "top level below the last bed" possible.
  `domain/drag_drop.dart` (pure, 9 tests) + `widgets/note_arrange_view.dart`.
- ⚠ **Hard-won gotcha:** the first drag build was broken because the drop
  indicator was inserted *between* rows and the dragged row shrank — the
  children list changed shape mid-drag, and with unkeyed children that
  reshuffled every element, detached the GlobalKeys used for measurement, and
  cancelled the hold-to-enter timer. **Keep the arrange row list structurally
  stable during a drag**: keyed rows, measurement key outside the draggable,
  same-footprint `childWhenDragging`, indicator painted in an overlay, pointer
  tracked via `Listener`. `note_arrange_view_test.dart` drives real gestures and
  fails on the old version — don't delete it.
- 184 tests green, analyze clean. **Known issue + polish owed → NOTES-ROADMAP.**

## 🏠 2026-08-02→03 — Home v2 + v2.1 (BUILT; v1.13.0 released, v2.1 UNRELEASED)
Specs: `specs/2026-08-02-home-v2-foundation-design.md`,
`specs/2026-08-03-home-control-pack-design.md`.
- **v1.13.0 (released):** task detail sheet (tap looks — only the circle
  completes; links to list + source note; TaskModel gained sourceNoteBlockId);
  Home layout → DB (schema **v21** home_blocks, seeded from prefs, in backups);
  pinned-note block, upgraded to a **live mini-editor** (real
  TextBlockView/CheckboxBlockView on Home; HeadingLineView +
  indentForInsertAfter extracted to be shared).
- **v2.1 (on master, unreleased):** per-block ⚙ options in Edit Home
  (items-shown cap + "+N more" — de-dupe claims only SHOWN tasks now;
  This-week days; hide-when-empty; collapse chevron on every header, persisted
  in configJson); new blocks **list / label / habits / shift** (multiInstance =
  {pinnedNote, list, label}); **responsive Home** — ≥720dp 2 columns, ≥1080dp 3,
  `distributeRoundRobin` round-robin in user order, drag-reorder single-column
  only. Completes queue items **#24 + #27**.

## 📖 2026-08-04 — Codex + My Workouts (RELEASED v1.14.0)
- **Codex** (`lib/features/codex/`): in-app manual, drawer → Codex. ~32 topics,
  10 areas, static Dart content, pure `searchCodex`/`hiddenGems` + content-sanity
  tests. 💡 Hidden gems filter.
  ⚠ **STANDING RULE (CLAUDE.md Working Style #11 + Definition of Done):** every
  user-facing change updates `codex_content.dart` in the SAME batch — new topic
  or edited one, `hidden: true` for undiscoverable things, `keywords` in the
  user's phrasing. Describe what the code does, never the intent.
- **My Workouts** (schema **v22** `programs.isTemplate`): the user's own single
  workouts on Workout home — New → name → normal session editor → tap to start,
  ✎ edit, long-press delete. Stored as sessions of a hidden container program
  (`ProgramRepository.kMyWorkoutsContainer`), filtered out of the program list
  AND the program-template shelf, so no new tables were needed.
  ⚠ The user first said "workout templates" and meant SINGLE workouts, not
  programs — the program-level ⋮ "Save as my template" also exists but is the
  secondary path.
- 221 tests green. CLAUDE.md refreshed: state header → v1.14.0/schema v22,
  feature rows for Codex / Home v2+v2.1 / personal templates, schema v19–v22
  notes, codex folder in the structure map.

## 🗺 FUTURE — Notes roadmap
User asked (2026-07-29) to capture next ideas: minimize photos, choose grid
cover photo, favourite templates/notes, sort, search inside notes, collapse
sections under a heading, faster in-note reorder — plus brainstormed adjacent
ideas. Full sized list at **`docs/superpowers/NOTES-ROADMAP.md`**. Suggested
first cluster: collapse-under-headings + fast reorder + shrink/gallery photos;
then search; favourites/cover are cheap wins.

## 🛠 DEVICE / TOOLING GOTCHAS (verbatim — these keep getting rediscovered)
- **Flip 6 `adb screencap` returns BLACK** (Impeller hardware overlay). Working
  capture: `screenrecord --time-limit 2 /sdcard/r.mp4` → pull → extract last
  frame with **opencv** (`cv2.VideoCapture`). `service call SurfaceFlinger 1008
  i32 1` un-blacks screencap on the emulator but NOT the phone.
- Screenshot coords: displayed 818px wide = real 1080px → **multiply by 1.32**.
- **Prefix adb `/sdcard` commands with `MSYS_NO_PATHCONV=1`** (Git Bash mangles
  `/sdcard` → `C:/Program Files/Git/sdcard`).
- USB drops when the phone sleeps → `adb shell svc power stayon usb`. If the
  device vanishes from `adb devices` entirely, it's usually USB mode =
  "Charging only" (needs File-transfer/MTP) or a charge-only cable.
- Tools: adb = `%LOCALAPPDATA%\Android\Sdk\platform-tools\adb.exe`;
  gh = `%LOCALAPPDATA%\Programs\gh-cli\bin\gh.exe` (auth manjo00);
  keytool/apksigner via `C:\Program Files\Android\Android Studio\jbr` +
  build-tools `lib/apksigner.jar`.
- Release APK is **debug-signed** (cert SHA-256 `94392f58…f94ee6b3`, CN=Android
  Debug) → `adb install -r` updates in place, no data loss.
- Release ritual: `flutter build apk --release` → `gh release create vX.Y.Z
  --repo manjo00/uplan-releases --title … --notes-file … <apk>` (APK asset
  mandatory, must not be a draft, tag semver > installed) → `adb install -r`.
